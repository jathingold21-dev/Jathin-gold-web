---
import { processSteps } from '../data/process';
---

<div class="steps">
  {processSteps.map((s) => (
    <div class="step">
      <div class="step-n">{s.n}</div>
      <div>
        <h3>{s.title}</h3>
        <p>{s.body}</p>
      </div>
    </div>
  ))}
</div>
